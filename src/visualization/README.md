# Visualization

Development and robustness plotting remains alongside the corresponding
analysis code in `src/modeling/run_prompt3.py` and `run_prompt4.py`.
`scripts/reproduce_figures.py` creates an aggregate alert-budget figure from a
local aggregate results file without exporting encounter-level data.
